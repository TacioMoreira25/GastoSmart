<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GastoSmart.App.SaveTransactionPage"
             Title="Salvar Transação"
             BackgroundColor="{StaticResource Gray950}">
    <VerticalStackLayout Spacing="20" Padding="20" VerticalOptions="Center">
        <Label Text="Página de Salvar Transação" 
               TextColor="White"
               FontSize="24"
               HorizontalOptions="Center" />
               
        <Label Text="Aqui o usuário pode revisar os dados do recibo extraídos pela API antes de salvar." 
               TextColor="{StaticResource Gray200}"
               HorizontalOptions="Center" 
               HorizontalTextAlignment="Center"/>
               
        <Button Text="Voltar" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.GoBackCommand}" />
    </VerticalStackLayout>
</ContentPage>
